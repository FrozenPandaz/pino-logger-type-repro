{
  "name": "pino-logger-type-repro",
  "version": "1.0.0",
  "description": "Reproduction showing pino version override breaking FastifyBaseLogger",
  "scripts": {
    "reproduce": "npx tsc --noEmit fastify-override-test.ts"
  },
  "dependencies": {
    "fastify": "^5.4.0",
    "pino": "9.8.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "overrides": {
    "pino": "9.8.0"
  }
}