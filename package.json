{
  "name": "pino-logger-type-repro",
  "version": "1.0.0",
  "description": "Minimal reproduction: BaseLogger.child() method missing in pino types",
  "scripts": {
    "reproduce": "npx tsc --noEmit simple.ts"
  },
  "dependencies": {
    "pino": "^9.8.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}